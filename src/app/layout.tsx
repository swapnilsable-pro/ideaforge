import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IdeaForge | AI-Powered Business Idea Generator",
  description: "Transform unsolved global challenges into actionable business opportunities. Generate validated startup ideas from XPrize, UN SDGs, WEF Global Risks, and Y Combinator's wishlist.",
  keywords: ["startup ideas", "business generator", "AI", "entrepreneurship", "XPrize", "SDGs", "innovation"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
